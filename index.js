
var romanToInt = function (s) {
    const dic = { I: 1, V: 5, X: 10, L: 50, C: 100, D: 500, M: 1000 };

    let length = s.length;
    let i = 0;
    let sum = 0;
    while (i < length) {

        if (i < length - 1) {

            if (s[dic[i]] > s[dic[i + 1]]) {

            }

        }

    }

    return sum;
};